<header>
    <nav>
      <a href="#" class="close__link" id="close__link" >
        <i class="fa-solid fa-xmark close__icon"></i>
      </a>
    </nav>
  </header>

  <style>
    header{
      width: 100%;
      background: linear-gradient(rgb(255, 255, 255) 0%, rgba(255, 255, 255, 0) 100%);
      padding: 2rem;
      height: 20vh;
      position: fixed;
      z-index: 1;
    }
    nav{
      position: relative;
    }

    .close__icon{
      background-color: #e8e5e480;
      color: #000;
      height: 45px;
      width: 45px;
      padding: .5em 1em;
      border-radius: 50%;
      display: flex;
      justify-content: center;
      align-items: center;
      font-size: 1.2rem;
      position: absolute;
      left: 50%;
      top: 50%;
      transform: translate(-50%, -50%);
    }
    .close__icon:hover{
      background-color: #000;
      color: #fff;
    }

  </style>